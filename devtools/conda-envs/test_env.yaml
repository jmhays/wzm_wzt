name: test
channels:
dependencies:
    # Base depends
  - python
  - pip


    # Testing
  - pytest
  - pytest-cov

  # Pip-only installs
  - pip:
    - codecov
    - numpy
    - sphinx
    - networkx
    - setuptools
    - cmake
    - mpi4py

